function plus(firstNumber, secondNumber) {
    console.log(firstNumber + secondNumber);
}
function divide(a, b) {
    console.log(a / b);
}

plus(80, 6);
divide(98, 20);
//(첫번째 변수인)firstNumber에는 첫번째 argument(=80)의 데이터가 전달되고,
//(두번째 변수인)secondNumber에는 두번째 argument(=6)이 전달된다.
//순서 아주 중요!!!
